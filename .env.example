# Copy this file to .env.local and paste your Anthropic API key after the = sign.
# Get a key at https://console.anthropic.com/ (Settings > API keys).
# Without a key, the app still works and shows sample feedback.
ANTHROPIC_API_KEY=
